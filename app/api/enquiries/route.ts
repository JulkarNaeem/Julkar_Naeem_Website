import {database} from '@/lib/database';
import {services,deliverables,formats} from '@/lib/content';
export async function POST(request:Request){const reply=(data:object,status=200)=>Response.json(data,{status,headers:{'Cache-Control':'no-store','X-Content-Type-Options':'nosniff'}});const origin=request.headers.get('origin');if(!origin||origin!==new URL(request.url).origin)return reply({error:'Please send the enquiry from this website.'},403);if(!request.headers.get('content-type')?.includes('application/json'))return reply({error:'Unsupported request format.'},415);if(Number(request.headers.get('content-length')||0)>24000)return reply({error:'The enquiry is too long.'},413);let data:any;try{const raw=await request.text();if(raw.length>24000)return reply({error:'The enquiry is too long.'},413);data=JSON.parse(raw)}catch{return reply({error:'Please check the enquiry and try again.'},400)}if(!data||typeof data!=='object'||Array.isArray(data))return reply({error:'Invalid enquiry.'},400);if(data.website)return reply({error:'The enquiry could not be accepted.'},400);const str=(name:string,max=200)=>typeof data[name]==='string'&&data[name].trim().length>0&&data[name].length<=max;const projectTypes=['PEB / portal frame','Multi-storey steel','Industrial structure','Platform / walkway','Stairs / handrails','Mixed or other steelwork'];if(!str('name')||!str('company')||!str('country')||!str('email')||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(data.email)||!str('message',6000)||data.message.trim().length<20||data.consent!==true||!projectTypes.includes(data.projectType)||typeof data.submissionId!=='string'||!/^[0-9a-f-]{36}$/.test(data.submissionId))return reply({error:'Check the required fields, business email, project type, message (at least 20 characters) and consent.'},400);for(const [key,allowed]of [['services',services.map(x=>x.title)],['deliverables',deliverables],['formats',formats]] as [string,string[]][]){if(!Array.isArray(data[key])||data[key].length>allowed.length||data[key].some((v:unknown)=>typeof v!=='string'||!allowed.includes(v)))return reply({error:'Choose valid services, deliverables and file formats.'},400)}for(const key of ['scale','schedule'])if(typeof data[key]!=='string'||data[key].length>300)return reply({error:'Please keep scale and schedule descriptions under 300 characters.'},400);try{const db=database();const now=new Date().toISOString();const ip=request.headers.get('cf-connecting-ip')||'local';const bytes=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(ip+now.slice(0,10)));const hash=Array.from(new Uint8Array(bytes)).map(x=>x.toString(16).padStart(2,'0')).join('');const recent=await db.prepare('SELECT count(*) AS total FROM enquiries WHERE request_hash = ? AND created_at > ?').bind(hash,new Date(Date.now()-3600000).toISOString()).first<{total:number}>();if((recent?.total||0)>=5)return reply({error:'Several enquiries have been received. Please try again in an hour.'},429);const details=JSON.stringify({scale:data.scale,schedule:data.schedule,services:data.services,deliverables:data.deliverables,formats:data.formats,message:data.message});await db.prepare('INSERT INTO enquiries (id,created_at,name,company,email,country,project_type,details,consent_at,request_hash) VALUES (?,?,?,?,?,?,?,?,?,?) ON CONFLICT(id) DO NOTHING').bind(data.submissionId,now,data.name.trim(),data.company.trim(),data.email.trim(),data.country.trim(),data.projectType,details,now,hash).run();return reply({ok:true,reference:data.submissionId.slice(0,8).toUpperCase()},201)}catch{return reply({error:'Your enquiry could not be saved at the moment. Your information is still in the form; please try again.'},503)}}
